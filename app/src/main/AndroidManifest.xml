<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test.minigamespear">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lable_1_"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EvolvingActivity" />
        <activity android:name=".DSLWactivity" />
        <activity android:name=".GallowsActivity" />
        <activity android:name=".StatisticMenuActivity"></activity>
        <activity android:name=".EYCmenu"></activity>
    </application>

</manifest>